export class Element {
    constructor (id:any, name:string, price:number, done:boolean){
   this.id = id;
   this.product = name;
   this.price = price;
   this.done = done;
    }
    public id: number; 
    public product: string;
    public price: number;
    public done: boolean;
   }
   